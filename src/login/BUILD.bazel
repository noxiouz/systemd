# SPDX-License-Identifier: LGPL-2.1-or-later

"""BUILD file for login tools."""

load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library")
load("//bazel/rules:gperf.bzl", "gperf")

package(default_visibility = ["//visibility:public"])

# ==============================================================================
# Code Generation
# ==============================================================================

gperf(
    name = "logind_gperf_c",
    src = "logind-gperf.gperf",
    out = "logind-gperf.c",
)

# ==============================================================================
# Logind library (shared code)
# ==============================================================================

LOGIND_SRCS = [
    "logind-action.c",
    "logind-brightness.c",
    "logind-button.c",
    "logind-core.c",
    "logind-dbus.c",
    "logind-device.c",
    "logind-inhibit.c",
    "logind-polkit.c",
    "logind-seat-dbus.c",
    "logind-seat.c",
    "logind-session-dbus.c",
    "logind-session-device.c",
    "logind-session.c",
    "logind-user-dbus.c",
    "logind-user.c",
    "logind-utmp.c",
    "logind-varlink.c",
    "logind-wall.c",
]

cc_library(
    name = "logind_lib",
    srcs = LOGIND_SRCS + [":logind_gperf_c"],
    hdrs = glob(["*.h"]),
    copts = [
        "-include", "$(GENDIR)/bazel/config.h",
        "-I$(GENDIR)/src/login",
    ],
    includes = ["."],
    deps = [
        "//bazel:config",
        "//src/shared:shared_static",
    ],
)

# ==============================================================================
# systemd-logind daemon
# ==============================================================================

cc_binary(
    name = "systemd-logind",
    srcs = ["logind.c"],
    copts = [
        "-include", "$(GENDIR)/bazel/config.h",
        "-I$(GENDIR)/src/login",
    ],
    deps = [
        "//bazel:config",
        ":logind_lib",
        "//src/shared:shared_static",
    ],
)

# ==============================================================================
# loginctl CLI
# ==============================================================================

cc_binary(
    name = "loginctl",
    srcs = [
        "loginctl.c",
        "sysfs-show.c",
        "sysfs-show.h",
    ],
    copts = [
        "-include", "$(GENDIR)/bazel/config.h",
    ],
    deps = [
        "//bazel:config",
        "//src/shared:shared_static",
    ],
)

# ==============================================================================
# systemd-inhibit
# ==============================================================================

cc_binary(
    name = "systemd-inhibit",
    srcs = ["inhibit.c"],
    copts = [
        "-include", "$(GENDIR)/bazel/config.h",
    ],
    deps = [
        "//bazel:config",
        "//src/shared:shared_static",
    ],
)

# ==============================================================================
# systemd-user-runtime-dir
# ==============================================================================

cc_binary(
    name = "systemd-user-runtime-dir",
    srcs = ["user-runtime-dir.c"],
    copts = [
        "-include", "$(GENDIR)/bazel/config.h",
    ],
    deps = [
        "//bazel:config",
        "//src/shared:shared_static",
    ],
)
