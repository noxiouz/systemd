# SPDX-License-Identifier: LGPL-2.1-or-later

"""BUILD file for version header generation."""

package(default_visibility = ["//visibility:public"])

genrule(
    name = "version_h",
    srcs = ["version.h.in"],
    outs = ["version.h"],
    cmd = "sed 's/@VCS_TAG@//' $< > $@",
)
